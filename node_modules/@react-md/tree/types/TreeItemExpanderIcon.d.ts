import { ReactElement, ReactNode } from "react";
import { IconRotatorBaseProps } from "@react-md/icon";
export interface TreeItemExpanderIconProps extends IconRotatorBaseProps {
    children?: ReactNode;
}
/**
 * The `TreeItemExpanderIcon` is a simple wrapper of the `IconRotator` prop to
 * be used within a `TreeView`.
 */
export declare function TreeItemExpanderIcon({ className, children, rotated, ...props }: TreeItemExpanderIconProps): ReactElement;
