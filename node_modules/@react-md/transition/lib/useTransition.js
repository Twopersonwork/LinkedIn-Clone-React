"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useTransition = void 0;
var react_1 = require("react");
var utils_1 = require("@react-md/utils");
var constants_1 = require("./constants");
var getNextStage_1 = require("./getNextStage");
var getTimeout_1 = require("./getTimeout");
var getTimeoutDuration_1 = require("./getTimeoutDuration");
/**
 *
 * @internal
 */
var reducer = function (state, action) {
    switch (action) {
        case constants_1.ENTER:
        case constants_1.ENTERING:
        case constants_1.EXIT:
        case constants_1.EXITING:
        case constants_1.EXITED:
            return __assign(__assign({}, state), { rendered: true, stage: action });
        case constants_1.ENTERED:
            return { rendered: true, stage: action, appearing: false };
        case constants_1.UNMOUNT:
            return { stage: constants_1.EXITED, rendered: false, appearing: false };
        default:
            return state;
    }
};
/**
 *
 * @internal
 */
var INITIAL_STATE = {
    appearing: false,
    rendered: true,
    stage: constants_1.ENTERED,
};
/**
 *
 * @internal
 */
var getInitialState = function (transitionIn, temporary, appear) { return function () {
    return {
        rendered: !temporary || transitionIn,
        appearing: appear && transitionIn,
        stage: transitionIn && !appear ? constants_1.ENTERED : constants_1.EXITED,
    };
}; };
/**
 * This is heavily inspired by the `Transition` component from
 * `react-transition-group` since it's really just a hook version of it.
 *
 * This hook allows you to transition between an enter and exit state with
 * defined timeouts, but you'll most likely be looking for the
 * `useCSSTransition` instead.
 *
 * @param options - All the options used for the transition.
 * @returns An object describing the current transition stage and props that
 * should be passed to a component.
 */
function useTransition(_a) {
    var _b = _a.appear, appear = _b === void 0 ? false : _b, _c = _a.repaint, repaint = _c === void 0 ? false : _c, _d = _a.temporary, temporary = _d === void 0 ? false : _d, transitionIn = _a.transitionIn, propTimeout = _a.timeout, onEnter = _a.onEnter, onEntering = _a.onEntering, onEntered = _a.onEntered, onExit = _a.onExit, onExiting = _a.onExiting, onExited = _a.onExited, propRef = _a.ref;
    var _e = react_1.useReducer(reducer, INITIAL_STATE, getInitialState(transitionIn, temporary, appear)), _f = _e[0], stage = _f.stage, rendered = _f.rendered, appearing = _f.appearing, dispatch = _e[1];
    // need to store in mutable ref since these are mostly going to be arrow
    // functions and shouldn't cause the transitions to change
    var handlers = react_1.useRef({
        onEnter: onEnter,
        onEntering: onEntering,
        onEntered: onEntered,
        onExit: onExit,
        onExiting: onExiting,
        onExited: onExited,
    });
    react_1.useEffect(function () {
        handlers.current = {
            onEnter: onEnter,
            onEntering: onEntering,
            onEntered: onEntered,
            onExit: onExit,
            onExiting: onExiting,
            onExited: onExited,
        };
    });
    var timeout = getTimeout_1.getTimeout(propTimeout, appear);
    var _g = utils_1.useEnsuredRef(propRef), nodeRef = _g[0], refHandler = _g[1];
    var disableEnterExitTransition = react_1.useRef(!appear || !transitionIn);
    // this effect handles the transition from:
    // - EXITED -> ENTER
    // - ENTERED -> EXIT
    // - EXITED -> ENTERED (when enter timeout is 0)
    // - ENTERED -> EXITED (when exit timeout is 0)
    //
    // all the dependencies are ignored except for `transitionIn` since the
    // other values changing would actually cause more bugs and this effect
    // really doesn't care if they changed.
    react_1.useEffect(function () {
        // should never trigger a transition on mount unless both the `appear` and
        // `transitionIn` are enabled for the appear transition
        if (disableEnterExitTransition.current) {
            disableEnterExitTransition.current = false;
            return;
        }
        var duration = 0;
        if (transitionIn) {
            duration = appearing ? timeout.appear : timeout.enter;
        }
        else {
            duration = timeout.exit;
        }
        if (duration > 0) {
            dispatch(transitionIn ? constants_1.ENTER : constants_1.EXIT);
        }
        else {
            dispatch(transitionIn ? constants_1.ENTERED : constants_1.EXITED);
        }
        // see comment above about why it's only `transitionIn`
        // eslint-disable-next-line react-hooks/exhaustive-deps
    }, [transitionIn]);
    var isFirstRender = react_1.useRef(true);
    // this effect handles the transitions for:
    // - ENTER -> ENTERING
    // - ENTERING -> ENTERED
    // - EXIT -> EXITING
    // - EXITING -> EXIT
    react_1.useEffect(function () {
        // need to skip the effects on first render since it should only be called
        // after a transition change
        if (isFirstRender.current) {
            isFirstRender.current = false;
            return;
        }
        var node = nodeRef.current;
        if (node) {
            if (repaint && stage !== constants_1.EXITED && stage !== constants_1.ENTERED) {
                // force repaint for CSS transitions
                // eslint-disable-next-line no-unused-expressions
                node.scrollTop;
            }
            var _a = handlers.current, onEnter_1 = _a.onEnter, onEntering_1 = _a.onEntering, onEntered_1 = _a.onEntered, onExit_1 = _a.onExit, onExiting_1 = _a.onExiting, onExited_1 = _a.onExited;
            switch (stage) {
                case constants_1.ENTER:
                    if (onEnter_1) {
                        onEnter_1(node, appearing);
                    }
                    break;
                case constants_1.ENTERING:
                    if (onEntering_1) {
                        onEntering_1(node, appearing);
                    }
                    break;
                case constants_1.ENTERED:
                    if (onEntered_1) {
                        onEntered_1(node, appearing);
                    }
                    break;
                case constants_1.EXIT:
                    if (onExit_1) {
                        onExit_1(node);
                    }
                    break;
                case constants_1.EXITING:
                    if (onExiting_1) {
                        onExiting_1(node);
                    }
                    break;
                case constants_1.EXITED:
                    if (onExited_1) {
                        onExited_1(node);
                    }
                    break;
                // no default
            }
        }
        var nextStage = getNextStage_1.getNextStage(stage);
        if (stage === nextStage) {
            if (stage === constants_1.EXITED && temporary) {
                dispatch(constants_1.UNMOUNT);
            }
            return;
        }
        var duration = getTimeoutDuration_1.getTimeoutDuration(stage, timeout.appear, timeout.enter, timeout.exit, appearing);
        if (duration <= 0) {
            dispatch(nextStage);
            return;
        }
        var dispatchTimeout = window.setTimeout(function () {
            dispatch(nextStage);
        }, duration);
        return function () {
            window.clearTimeout(dispatchTimeout);
        };
    }, [
        nodeRef,
        appearing,
        repaint,
        stage,
        temporary,
        timeout.appear,
        timeout.enter,
        timeout.exit,
    ]);
    return {
        ref: refHandler,
        stage: stage,
        rendered: rendered,
        appearing: appearing,
        dispatch: dispatch,
    };
}
exports.useTransition = useTransition;
//# sourceMappingURL=useTransition.js.map