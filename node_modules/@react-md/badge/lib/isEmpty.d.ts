import { ReactNode } from "react";
/**
 * A small util to check if the badge is considered empty.
 *
 * @internal
 */
export declare function isEmpty(children: ReactNode, disableNullOnZero: boolean): boolean;
