"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useTabsMovement = void 0;
var react_1 = require("react");
var utils_1 = require("@react-md/utils");
/**
 * @internal
 */
function useTabsMovement(_a) {
    var onClick = _a.onClick, onKeyDown = _a.onKeyDown, children = _a.children, automatic = _a.automatic, horizontal = _a.horizontal, activeIndex = _a.activeIndex, onActiveIndexChange = _a.onActiveIndexChange;
    // first filter out all "nulled" or falsish elements since it's possible to do:
    // {!thing && <Tab />}
    var tabs = react_1.useMemo(function () { return react_1.Children.toArray(children).filter(Boolean); }, [
        children,
    ]);
    // now filter out any other invalid elements (text nodes for some reason) and
    // disabled tabs since they shouldn't be keyboard focusable
    var visibleTabs = react_1.useMemo(function () {
        return tabs.filter(function (child) { return react_1.isValidElement(child) && !child.props.disabled; });
    }, [tabs]);
    var _b = react_1.useState(activeIndex), focusedIndex = _b[0], setFocusedIndex = _b[1];
    react_1.useEffect(function () {
        setFocusedIndex(activeIndex);
    }, [activeIndex]);
    var _c = utils_1.useKeyboardMovement(__assign(__assign({}, (horizontal
        ? utils_1.MovementPresets.HORIZONTAL_TABS
        : utils_1.MovementPresets.VERTICAL_TABS)), { onKeyDown: onKeyDown,
        focusedIndex: focusedIndex, items: visibleTabs, onChange: function (_a) {
            var index = _a.index;
            if (index === -1) {
                return;
            }
            if (automatic) {
                onActiveIndexChange(index);
            }
            var item = itemRefs[index] && itemRefs[index].current;
            if (item) {
                item.focus();
            }
            setFocusedIndex(index);
        } })), itemRefs = _c[0], handleKeyDown = _c[1];
    var handleClick = react_1.useCallback(function (event) {
        if (onClick) {
            onClick(event);
        }
        var target = event.target && event.target.closest("button");
        if (!target) {
            return;
        }
        var index = itemRefs.findIndex(function (ref) { return ref.current === target; });
        if (activeIndex !== index) {
            onActiveIndexChange(index);
        }
    }, [activeIndex, itemRefs, onActiveIndexChange, onClick]);
    return {
        tabs: tabs,
        itemRefs: itemRefs,
        handleClick: handleClick,
        handleKeyDown: handleKeyDown,
    };
}
exports.useTabsMovement = useTabsMovement;
//# sourceMappingURL=useTabsMovement.js.map