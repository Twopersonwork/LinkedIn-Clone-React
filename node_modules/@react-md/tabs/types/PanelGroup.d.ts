import { ReactElement, ReactNode } from "react";
export interface PanelGroupProps {
    children?: ReactNode;
    persistent: boolean;
    disableTransition: boolean;
}
/**
 * @internal
 */
export declare function PanelGroup({ persistent, disableTransition, children, }: PanelGroupProps): ReactElement;
