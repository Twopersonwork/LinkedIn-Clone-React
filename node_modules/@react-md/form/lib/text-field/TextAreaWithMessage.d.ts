import React from "react";
import { FieldMessageContainerExtension } from "../FormMessageContainer";
import { TextAreaProps } from "./TextArea";
/**
 * @remarks \@since 2.5.0
 */
export declare type TextAreaWithMessageProps = FieldMessageContainerExtension<TextAreaProps>;
/**
 * This component is a simple wrapper for the `TextArea` and `FormMessage`
 * components that should be used along with the `useTextField` hook to
 * conditionally show help and error messages with a `TextArea`.
 *
 * Simple example:
 *
 * ```ts
 * const [value, areaProps] = useTextField({
 *   id: "area-id",
 * });
 *
 * return (
 *   <TextFieldWithMessage
 *     label="Label"
 *     placeholder="Placeholder"
 *     {...areaProps}
 *   />
 * );
 * ```
 *
 * @remarks \@since 2.5.0
 */
export declare const TextAreaWithMessage: React.ForwardRefExoticComponent<TextAreaProps & {
    messageProps?: (import("..").FormMessageProps & {
        ref?: React.Ref<HTMLDivElement> | undefined;
    }) | undefined;
    messageContainerProps?: (React.HTMLAttributes<HTMLDivElement> & {
        ref?: React.Ref<HTMLDivElement> | undefined;
    }) | undefined;
} & React.RefAttributes<HTMLTextAreaElement>>;
