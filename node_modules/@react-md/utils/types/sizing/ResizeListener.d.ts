import { ResizeListenerOptions } from "./useResizeListener";
export declare type ResizeListenerProps = Omit<ResizeListenerOptions, "enabled">;
/**
 * This is a simple component that will attach a throttled resize event listener
 * when mounted, and detach when it unmounts.
 *
 * This component only works for entire app resize events. If you are looking
 * for specific element resize events, check out the `ResizeObserver` component
 * instead.
 */
export declare function ResizeListener({ onResize, options, immediate, }: ResizeListenerProps): null;
export declare namespace ResizeListener {
    var propTypes: {
        onResize: any;
        options: any;
        immediate: any;
    };
}
